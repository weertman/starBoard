# Deep Learning dependencies for starBoard
# Install with: pip install -r requirements-dl.txt

# Core ML libraries
torch>=2.0.0
torchvision>=0.15.0

# Model loading (HuggingFace transformers for Swin backbone)
transformers>=4.30.0

# YOLO instance segmentation (for star cropping)
ultralytics>=8.0.0
opencv-python>=4.8.0

# Image augmentations
albumentations>=1.3.0

# Data processing
pandas>=1.5.0
scikit-learn>=1.2.0

# Already in main requirements but listed for completeness
numpy>=1.24.0
Pillow>=9.4.0
tqdm>=4.65.0

# Optional: GPU acceleration
# For CUDA support, install PyTorch with CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

